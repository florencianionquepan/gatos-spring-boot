package com.example.gatosspringboot.service.interfaces;

import com.example.gatosspringboot.model.Cuota;

public interface ICuotaService {

    String creacionPreferencia(Cuota cuota);
    Cuota nuevaCuota(Cuota cuota);
}
